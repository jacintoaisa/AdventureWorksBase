@model IEnumerable<AdventureWorks.ViewModel.ClaseResumen>

@{
    ViewData["Title"] = "View9";
}

<h1>View9</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreColor)
            </th>
            <th>
                resumen
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreColor)
            </td>
            <td>
                @foreach (var hijo in item.hijas)
                {
                    @Html.DisplayForModel(hijo.Edad)

                }
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
